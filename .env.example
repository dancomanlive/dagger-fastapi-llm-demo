# Dagger needs credentials for the underlying LLM Provider (e.g., OpenAI)
# Replace with your actual OpenAI API Key
OPENAI_API_KEY="sk-..."

# Optional: Define the model to use
LLM_MODEL="gpt-4o-mini"

DAGGER_CLOUD_TOKEN="dag_..."
DOCKERHUB_USERNAME="..."
DOCKERHUB_TOKEN="..."